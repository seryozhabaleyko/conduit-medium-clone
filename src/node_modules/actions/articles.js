import { API } from 'constants/index';

const token = localStorage.getItem('jwt');

export const fetchArticles = (limit = 10, offset) => async (dispatch) => {

    dispatch({
        type: 'FETCH_ARTICLES_REQUSET',
    });

    try {
        const url = `${API}/articles?limit=${limit}&offset=${offset ? offset * limit : 0}`;
        const options = {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'authorization': token ? `Token ${token}` : '',
            },
        };
        const response = await fetch(url, options);
        const result = await response.json();

        dispatch({
            type: 'FETCH_ARTICLES_SUCCESS',
            payload: result,
        });
    } catch (error) {
        dispatch({
            type: 'FETCH_ARTICLES_FAILURE',
            payload: error,
        });
    }
};

export const fetchArticlesByAuthor = (author = '', page = 0, limit = 5) => async (dispatch) => {

    dispatch({
        type: 'FETCH_ARTICLES_BY_AUTHOR_REQUEST',
    });

    try {
        const url = `${API}/articles?author=${encodeURIComponent(author)}&limit=${limit}&offset=${page ? page * limit : 0}`;
        const options = {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'authorization': token ? `Token ${token}` : '',
            },
        };
        const response = await fetch(url, options);
        const result = await response.json();

        dispatch({
            type: 'FETCH_ARTICLES_BY_AUTHOR_SUCCESS',
            payload: result,
        });
    } catch (error) {
        dispatch({
            type: 'FETCH_ARTICLES_BY_AUTHOR_FAILURE',
            payload: error,
        });
    }
};

export const fetchYourFeedArticles = (limit = 10, offset = 0) => async (dispatch) => {

    dispatch({
        type: 'FETCH_ARTICLES_FEED_REQUEST',
    });

    try {
        const url = `${API}/articles/feed?limit=${limit}&offset=${offset}`;
        const options = {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'authorization': token ? `Token ${token}` : '',
            },
        };
        const response = await fetch(url, options);
        const result = await response.json();

        dispatch({
            type: 'FETCH_ARTICLES_FEED_SUCCESS',
            payload: result,
        });
    } catch (error) {
        dispatch({
            type: 'FETCH_ARTICLES_FEED_FAILURE',
            payload: error,
        });
    }
};

export const submitCreatedArticle = (article) => async (dispatch) => {

    dispatch({
        type: 'SUBMIT_CREATED_ARTICLE_REQUEST',
    });

    try {
        const url = `${API}/articles`;
        const options = {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'authorization': token ? `Token ${token}` : '',
            },
            body: JSON.stringify(article),
        };
        const response = await fetch(url, options);
        const result = await response.json();

        console.log(result);

        dispatch({
            type: 'SUBMIT_CREATED_ARTICLE_SUCCESS',
            payload: result,
        });
    } catch (error) {
        dispatch({
            type: 'SUBMIT_CREATED_ARTICLE_FAILURE',
            payload: error,
        });
    }
};