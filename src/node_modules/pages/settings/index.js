import React, { useEffect } from 'react';
import { connect } from 'react-redux';
import { Layout, Menu, Row, Col, Form, Input, Button } from 'antd';

import Layout1 from 'components/Layout';

import './settings.scss';

const { TextArea } = Input;
const { Content, Sider } = Layout;

function Settings({ match }) {

    const { slug } = match.params;

    useEffect(() => {
        document.title = 'Редактирование профиля - medium';
    });

    return (
        <Layout1>
            <section className="settings">
                <Layout style={{ padding: '24px 0', background: "#fff" }}>
                    <Sider
                        width={200}
                    >
                        <Menu
                            mode="inline"
                            defaultSelectedKeys={['1']}
                            style={{ height: '100%' }}
                        >
                            <Menu.Item key="1">Редактирование</Menu.Item>
                            <Menu.Item key="2">option2</Menu.Item>
                            <Menu.Item key="3">option3</Menu.Item>
                            <Menu.Item key="4">option4</Menu.Item>
                        </Menu>
                    </Sider>
                    <Content style={{ padding: '0 24px', minHeight: 280 }}>
                        <Form>
                            <Form.Item>
                                <Input
                                    placeholder="URL изображения профиля"
                                />
                            </Form.Item>
                            <Form.Item
                                name="username"
                                rules={[
                                    {
                                        required: true,
                                        message: 'Please input your Username!',
                                    },
                                ]}
                            >
                                <Input
                                    placeholder="Имя пользователя"
                                />
                            </Form.Item>
                            <Form.Item>
                                <TextArea
                                    autoSize={{ minRows: 8 }}
                                    placeholder="Краткая биография о вас"
                                />
                            </Form.Item>
                            <Form.Item
                                name="email"
                                rules={[
                                    {
                                        type: 'email',
                                        required: true,
                                        message: 'Email is not validate email!',
                                    },
                                ]}
                            >
                                <Input
                                    type="email"
                                    placeholder="Электронный адрес"
                                />
                            </Form.Item>
                            <Form.Item
                                name="password"
                                rules={[
                                    {
                                        required: true,
                                        message: 'Please input your Password!',
                                    },
                                ]}
                            >
                                <Input
                                    type="password"
                                    placeholder="Новый пароль"
                                />
                            </Form.Item>
                            <Form.Item>
                                <Button
                                    type="primary"
                                    htmlType="submit"
                                >
                                    Обновить настройки
                                </Button>
                            </Form.Item>
                        </Form>
                    </Content>
                </Layout>
            </section>
        </Layout1>
    );
}

const mapStateToProps = (state) => ({

});

const mapDispatchToProps = {

};

export default connect(mapStateToProps, mapDispatchToProps)(Settings);