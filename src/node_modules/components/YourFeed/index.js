import React, { useEffect } from 'react';
import { connect } from 'react-redux';
import PropTypes from 'prop-types';

import { fetchYourFeedArticles } from 'actions/articles';

function YourFeed({ fetchYourFeedArticles }) {

    useEffect(() => {
        fetchYourFeedArticles();
    }, [fetchYourFeedArticles]);

    return (
        <div>

        </div>
    );
}

YourFeed.propTypes = {
    fetchYourFeedArticles: PropTypes.func.isRequired,
};

const mapStateToProps = (state) => ({

});

const mapDispatchToProps = {
    fetchYourFeedArticles,
};

export default connect(mapStateToProps, mapDispatchToProps)(YourFeed);