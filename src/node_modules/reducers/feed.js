const initialState = {
    isFetching: false,
    articles: [],
    error: null,
    articlesCount: 0,
};

export default (state = initialState, { type, payload }) => {

    switch (type) {
        case 'FETCH_ARTICLES_FEED_REQUEST':
            return { ...state, isFetching: true };
        case 'FETCH_ARTICLES_FEED_SUCCESS':
            return { ...state, isFetching: false, ...payload };
        case 'FETCH_ARTICLES_FEED_FAILURE':
            return { ...state, isFetching: false, error: payload };

        case 'FETCH_LIKE_SUCCESS':
        case 'FETCH_UNLIKE_SUCCESS':
            return {
                ...state,
                articles: state.articles.map((article) => {

                    if (article.slug === payload.article.slug) {
                        return {
                            ...article,
                            favorited: payload.article.favorited,
                            favoritesCount: payload.article.favoritesCount
                        };
                    }

                    return article;
                }),
            };
        default:
            return state;
    }
};